# Assessment Report — osc-mcp

**Date**: 2026-07-21
**Repo Type**: Standard MCP Server (FastMCP 3.4.4) with webapp + Tauri native
**SOTA Score**: **47/100** (< 60 — RUNT, needs major work)

---

## Score Breakdown

Base: 100. Deductions:

| Category | Deduction | Detail |
|----------|-----------|--------|
| Required files | -28 | Missing .env.example, CLAUDE.md, justfile recipes, start.ps1 weak |
| Tool surface | -12 | No annotations, no skills, no resources, no prompts, print() in 9 files |
| Testing | -8 | Zero tests |
| Webapp SOTA | -16 | No zustand, no data-testid, no useZoom, no speech, chat incomplete |
| CORS | -15 | allow_origins=["*"] at 2 sites |
| Security | -15 | build.ps1 + tauri.conf.json bundle .env (not .env.example) |
| Tauri | -8 | No stream watching, free_port() single-pass, no stdout/stderr capture |
| FastMCP gaps | -12 | No skills provider, no @mcp.resource(), no @mcp.prompt(), no annotations |
| Bad patterns | -4 | print() in 9 app files, console.error in 10 frontend files |
| Error handling | -4 | No _error_response() pattern, no logger.exception() in shared helper |
| CI/CD | -8 | No .github/workflows/ci.yml |
| Dockerization | 0 (INFO) | Has docker-compose.yml + Dockerfile |

**Per-severity count**: CRITICAL=5, HIGH=6, MEDIUM=8, LOW=4

---

## 1A. Required Files

| File | Status | Notes |
|------|--------|-------|
| pyproject.toml | ✅ | Valid, FastMCP 3.4.4+, prefab-ui 0.14+ |
| justfile | ⚠️ | Has lint, fix, check-sec, audit-deps. **Missing**: serve, test, fmt, mcpb-pack, build-native, cua-nsis-test, e2e, gates-green, certify |
| llms.txt | ✅ | Links to docs/llms-full.txt |
| llms-full.txt | ✅ | 178 lines, comprehensive tool reference |
| glama.json | ✅ | Valid schema, 25 tools, dual transport |
| start.ps1 | ❌ | No port zombie clearing, no readiness TCP poll, no auto-open browser |
| start.bat | ✅ | Delegates to start.ps1 |
| uv.lock | ✅ | Committed |
| .gitignore | ⚠️ | Missing `*.mcpb`, `native/target/`, `native/gen/` |
| .env.example | ❌ | **MISSING** — not at repo root |
| .opencode/skills/ | ❌ | Missing |
| CHANGELOG.md | ✅ | Present |
| .cursorrules | ✅ | Present |
| AGENTS.md | ✅ | Present |
| CLAUDE.md | ❌ | **MISSING** — no per-repo CLAUDE.md |
| Git repo | ✅ | Has commits, has origin remote |
| .bak dross | ⚠️ | 8 .bak files found across repo |

## 1B. Tool Surface

| Check | Status | Detail |
|-------|--------|--------|
| help/status tool | ✅ | Has arxiv_help equivalent? Check: has oscilloscope help? Add status check |
| Portmanteau pattern | ✅ | Apps use portmanteau pattern (ableton_manager, vcv_manager) |
| `## Return Format` in docstrings | ❌ | server.py tools lack standard docstring sections |
| `## Examples` in docstrings | ❌ | No examples in tool docstrings |
| No `Args:` blocks | ❌ | server.py still uses `Args:` blocks |
| Prefab cards on list/status tools | ✅ | show_active_mappings, show_discovered_devices, etc. |
| Tool annotations (`annotations=`) | ❌ | Not used anywhere |
| FastMCP >=3.4.2,<4 | ✅ | 3.4.4 in pyproject.toml (but `[all]` extra doesn't exist) |
| `@mcp.resource()` | ❌ | Not used |
| `@mcp.prompt()` | ❌ | Not used |
| `ctx.sample()` | ✅ | Used in sampling.py (5 calls) |
| Skills directory with SKILL.md | ❌ | No skills directory |
| `run_server.py` dual transport | ✅ | Detects MCP_PORT → HTTP, fallback → stdio |
| HTTP daemon + stdio proxy | ⚠️ | Has run_server.py but uses run_http_async (drops CORS) |
| GET /api/health | ✅ | In api/main.py |
| Self-termination tool | ⚠️ | No dedicated shutdown tool, but lifespan handles cleanup |
| Tool naming convention | ✅ | Verb-led snake_case |
| `# type: ignore` without reason | ⚠️ | 1 occurrence in transport.py |

## 1C. Testing

| Check | Status |
|-------|--------|
| Has any tests | ❌ **None** — `tests/` directory exists but only contains `test_stdio_server.py` which needs pytest-asyncio. No pytest tests collected |
| Tests pass | ❌ N/A — pytest not installed? |
| Coverage config | ❌ No [tool.pytest.ini_options] for coverage |
| Playwright/E2E config | ❌ No playwright.config.ts |
| Playwright/E2E tests | ❌ No e2e/ directory |

## 1D. Webapp SOTA

| Check | Status | Detail |
|-------|--------|--------|
| Stack | ⚠️ | React 19 + Vite 7 + Tailwind + Lucide + Framer Motion. **Missing Zustand** (has @tanstack/react-query instead) |
| Dark theme | ✅ | CSS variables with dark HSL values. **Missing `color-scheme: dark`** |
| Pages | 14/16 | Has Dashboard, Chat, Tools, Settings, Help, Logging, Control, Visualizer, Apps Hub, plus app pages. **Missing Skills page, API Docs page** |
| Chat | ⚠️ | **Missing**: skill-first fetch, localStorage persistence, 4+ personalities (has 3), export, data-testid, example prompts |
| Local LLM glom-on | ⚠️ | Settings page has provider/model selectors. **Missing**: auto-detection on mount, GPU prompt |
| data-testid | ❌ | **Zero** data-testid attributes across entire frontend |
| Logger modal | ✅ | Ring buffer with filter/export/search in Logging.tsx |
| Help modal | ⚠️ | Has help page but no modal (dedicated /help route) |
| Keyboard shortcuts | ❌ | No useZoom hook, no Ctrl+Scroll zoom, no keyboard shortcuts |
| Error/empty/loading states | ⚠️ | Some pages have loading, some have error. Not universal |
| Backend framework | ✅ | FastAPI — correct per decision matrix |
| Font size & contrast | ⚠️ | Some app pages use debug `text-xs` patterns |
| Ctrl+Scroll zoom | ❌ | No useZoom hook, no zoom implementation |
| README completeness | ⚠️ | Has features/tools but missing Zustand from stack list |
| Exponential backoff | ❌ | Health check has no retry/backoff |

## 1E. REST API Endpoints

| Endpoint | Status | Detail |
|----------|--------|--------|
| GET /api/health | ✅ | In api/main.py and web_sota/backend/server.py |
| GET /api/status | ✅ | /api/v1/stats in api/main.py |
| POST /api/chat | ❌ | No chat endpoint |
| GET /api/skills | ❌ | No skills endpoint |
| GET /api/llm/discover | ⚠️ | Settings page calls /api/llm/providers |
| GET /docs | ❌ | No Swagger UI (but FastAPI auto-docs should work at /docs) |
| GET /api/v1/diagnostics | ❌ | Not present |
| Self-termination tool | ❌ | No shutdown endpoint/tool |

## 1F. CORS Audit

| Finding | Severity | Detail |
|---------|----------|--------|
| `allow_origins=["*"]` in api/main.py | **CRITICAL** | Line 14 |
| `allow_origins=["*"]` in web_sota/backend/server.py | **CRITICAL** | Line 57 |
| `mcp.run_http_async()` in transport.py | **CRITICAL** | Drops CORSMiddleware |
| No allow_origin_regex | HIGH | Missing Tailscale + LAN regex |
| Missing tauri://localhost origins | HIGH | Tauri WebView blocked |

## 1G. Security Audit

| Check | Status | Detail |
|-------|--------|--------|
| build.ps1 bundles .env not .env.example | **CRITICAL** | Lines 70-78 |
| tauri.conf.json resources includes .env | **CRITICAL** | Line 31 |
| .env.example missing | HIGH | None at repo root |
| .gitignore missing patterns | LOW | Missing *.mcpb, native/target/ |
| Hardcoded secrets | ✅ | None found |

## 1H. Tauri / Native Audit

| Check | Status | Detail |
|-------|--------|--------|
| backend.rs free_port() | ⚠️ | Single-pass taskkill, no multi-layer kill, no Stop-Process, no UAC, no 240s poll |
| backend.rs stream watching | ❌ | stdout/stderr set to Stdio::null() — no stream capture, no "ready" emit |
| backend.rs health poll | ✅ | TCP connect loop 30x2s |
| hooks.nsh | ✅ | Has both PREINSTALL + PREUNINSTALL, correct binary names |
| tauri.conf.json targets: ["nsis"] | ✅ | Correct |
| tauri.conf.json resources includes .env.example | ❌ | Includes .env instead |
| Cargo.toml deps | ✅ | shell, fs, process plugins |
| Frontend zoom | ❌ | No useZoom() hook |
| Backend-status listener | ⚠️ | main.rs has event emit but frontend doesn't listen |
| CUA smoke test | ✅ | scripts/cua-smoke.py + cua-nsis-config.json present |

## 1I. FastMCP Features

| Feature | Status | Detail |
|---------|--------|--------|
| FastMCP >=3.4.2 | ✅ | 3.4.4 (but `[all]` extra invalid) |
| ctx.sample() | ✅ | Used in sampling.py |
| Agentic workflow tools | ✅ | generate_osc_workflow with ctx |
| Skills provider | ❌ | Not implemented |
| @mcp.prompt() | ❌ | Not used |
| @mcp.resource() | ❌ | Not used |
| Prefab UI in core deps | ✅ | prefab-ui>=0.14.0 in pyproject.toml |
| Prefab @mcp.tool(app=True) | ✅ | 4 Prefab tools (show_active_mappings, etc.) |
| Tool annotations | ❌ | No annotations= on any tool |
| Dialogic returns | ✅ | Most tools use {"status","message",...} pattern |
| Pydantic v2 patterns | ✅ | Using model_validate etc. |

## 1J. Bad Pattern Scanning

| Pattern | Count | Severity |
|---------|-------|----------|
| Bare `except:` | 0 | Clean |
| `print()` in non-test Python | 35 across 9 files | MEDIUM |
| `console.log` in prod JS/TS | 0 | Clean |
| `console.error` in prod JS/TS | 10 | LOW (error handling only) |
| Hardcoded secrets | 0 | Clean |
| `# type: ignore` without error code | 1 (transport.py:154) | LOW |

## 1K. Error Handling

| Check | Status |
|-------|--------|
| No bare `except: pass` in startup/cleanup | ✅ Clean |
| Module-import functions raise on failure | ⚠️ Some return False patterns exist |
| `_error_response()` with `logger.exception()` | ❌ No shared error response helper |
| Logging configuration | ✅ Has getLogger(__name__) in all modules |
| `logger.exception()` vs `logger.error()` | ⚠️ Mix of both patterns |

## 1L. CI/CD

| Check | Status |
|-------|--------|
| .github/workflows/ci.yml | ❌ Only release.yml present |
| Has dependabot.yml | ✅ Present |
| Uses windows-latest runner | ❌ No CI |
| Has ruff check step | ❌ No CI |
| Has pytest step | ❌ No CI |

---

## Critical Items to Fix

1. **CORS `["*"]`** in `src/oscmcp/api/main.py` — replace with fleet origins + regex
2. **CORS `["*"]`** in `web_sota/backend/server.py` — replace with fleet origins + regex
3. **`mcp.run_http_async()`** in `transport.py` — replace with uvicorn.Server on mcp.http_app()
4. **`build.ps1` bundles `.env`** — switch to `.env.example`
5. **`tauri.conf.json` resources includes `.env`** — switch to `.env.example`

## High Items to Fix

1. Create `.env.example` at repo root
2. Fix `start.ps1` with port zombie clearing, health poll, auto-open
3. Add stream watching to `backend.rs` (stdout/stderr)
4. Fix `free_port()` with multi-layer kill + 240s poll
5. Add `allow_origin_regex` for Tailscale + LAN
6. Add test infrastructure
7. Add `CLAUDE.md` at repo root
